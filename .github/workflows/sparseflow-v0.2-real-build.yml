name: SparseFlow v0.2 - REAL Build & Test (⚠️ Takes ~20 minutes)

on:
  push:
    branches: [ master, main ]
    tags: [ 'v*' ]
  pull_request:
    branches: [ master, main ]
  workflow_dispatch:

jobs:
  real-build-and-test:
    name: Complete Build, Compile & Test Suite
    runs-on: ubuntu-22.04
    timeout-minutes: 30
    
    steps:
    - name: ⚠️ Warning - This Takes 20 Minutes
      run: |
        echo ""
        echo "╔════════════════════════════════════════════════════════════════╗"
        echo "║                                                                ║"
        echo "║         ⚠️  REAL BUILD & TEST - TAKES ~20 MINUTES ⚠️          ║"
        echo "║                                                                ║"
        echo "╚════════════════════════════════════════════════════════════════╝"
        echo ""
        echo "Starting build at: $(date)"
    
    - name: Checkout Repository
      uses: actions/checkout@v3
    
    - name: Install System Dependencies
      run: |
        echo "═══════════════════════════════════════════════════════════════"
        echo "📦 Installing System Dependencies"
        echo "═══════════════════════════════════════════════════════════════"
        sudo apt-get update
        sudo apt-get install -y build-essential cmake ninja-build git python3
        echo "✅ System dependencies installed"
    
    - name: Install LLVM/MLIR 19
      run: |
        echo "═══════════════════════════════════════════════════════════════"
        echo "�� Installing LLVM/MLIR 19 (~8 minutes)"
        echo "═══════════════════════════════════════════════════════════════"
        wget https://apt.llvm.org/llvm.sh
        chmod +x llvm.sh
        sudo ./llvm.sh 19
        sudo apt-get install -y llvm-19 llvm-19-dev clang-19 libmlir-19-dev mlir-19-tools
        echo "✅ LLVM/MLIR 19 installed"
    
    - name: Install OpenMP
      run: |
        sudo apt-get install -y libomp-dev
        echo "✅ OpenMP installed"
    
    - name: Build Compiler
      run: |
        echo "═══════════════════════════════════════════════════════════════"
        echo "🏗️  Building Compiler"
        echo "═══════════════════════════════════════════════════════════════"
        cd compiler && mkdir -p build && cd build
        cmake -DCMAKE_PREFIX_PATH=/usr/lib/llvm-19 \
              -DMLIR_DIR=/usr/lib/llvm-19/lib/cmake/mlir \
              -DLLVM_DIR=/usr/lib/llvm-19/lib/cmake/llvm \
              -DCMAKE_BUILD_TYPE=Release ..
        make -j$(nproc)
        
        if [ -f "passes/SparseFlowPasses.so" ]; then
          echo "✅ Compiler built successfully"
        else
          echo "❌ Compiler build FAILED"
          exit 1
        fi
    
    - name: Build Runtime
      run: |
        echo "═══════════════════════════════════════════════════════════════"
        echo "🏗️  Building Runtime"
        echo "═══════════════════════════════════════════════════════════════"
        cd runtime && mkdir -p build && cd build
        cmake -DCMAKE_BUILD_TYPE=Release ..
        make -j$(nproc)
        
        if [ -f "libsparseflow_runtime.so" ]; then
          echo "✅ Runtime built successfully"
        else
          echo "❌ Runtime build FAILED"
          exit 1
        fi
    
    - name: Run Runtime Tests
      run: |
        echo "═══════════════════════════════════════════════════════════════"
        echo "🧪 Running Runtime Tests"
        echo "═══════════════════════════════════════════════════════════════"
        cd runtime/build
        export LD_LIBRARY_PATH=$(pwd):$LD_LIBRARY_PATH
        
        if [ -f "test_nm_runtime" ]; then
          ./test_nm_runtime || exit 1
        fi
        
        if [ -f "test_pattern_validation" ]; then
          ./test_pattern_validation || exit 1
        fi
        
        echo "✅ Runtime tests passed"
    
    - name: Run MLIR Integration Tests
      run: |
        echo "═══════════════════════════════════════════════════════════════"
        echo "🔬 MLIR Integration Tests (REAL - will fail if broken!)"
        echo "═══════════════════════════════════════════════════════════════"
        ./scripts/run_mlir_integration_tests.sh
    
    - name: Final Summary
      run: |
        echo ""
        echo "╔════════════════════════════════════════════════════════════════╗"
        echo "║           ✅ REAL BUILD & TEST COMPLETED! ✅                   ║"
        echo "╚════════════════════════════════════════════════════════════════╝"
        echo ""
        echo "  ✅ LLVM/MLIR 19 installed"
        echo "  ✅ Compiler built from source"
        echo "  ✅ Runtime compiled"
        echo "  ✅ Runtime tests PASSED"
        echo "  ✅ MLIR tests PASSED"
        echo ""
        echo "Completed at: $(date)"
        echo ""
        echo "✨ All checks ACTUALLY passed - not fake green! ✨"
