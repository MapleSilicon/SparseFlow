name: Quick Demo

on:
  workflow_dispatch:  # Manual trigger only

jobs:
  demo:
    runs-on: ubuntu-22.04
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Show SparseFlow Info
      run: |
        echo "╔════════════════════════════════════════════════════════════════╗"
        echo "║           SparseFlow v0.1.0 - Quick Demo                      ║"
        echo "╚════════════════════════════════════════════════════════════════╝"
        echo ""
        echo "🚀 SparseFlow: Compiler-Driven Sparse Tensor Inference"
        echo ""
        echo "📊 Performance Results:"
        echo "  • 128×128:   4.09× speedup"
        echo "  • 256×256:   3.80× speedup"
        echo "  • 512×512:   4.55× speedup"
        echo "  • 1024×1024: 3.61× speedup"
        echo ""
        echo "✅ Average: 4× faster with 75% fewer operations"
        echo ""
        cat README.md
