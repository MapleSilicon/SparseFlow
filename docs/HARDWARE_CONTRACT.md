# SparseFlow Hardware Contract

## Overview
This document defines the JSON interface between SparseFlow analysis and hardware accelerators.

## JSON Schema

### Root Object
```json
{
  "version": "0.7.1",
  "operations": [Operation],
  "metadata": Metadata
}
